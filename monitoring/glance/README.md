# Glance

Installed using the Proxmox helper script, located [here](https://community-scripts.github.io/ProxmoxVE/scripts?id=glance). This script needs to run on the **host** console, so that it will create its own LXC to be installed and run on. 